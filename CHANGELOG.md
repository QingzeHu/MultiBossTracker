# Changelog

## 1.14.0 — 2026-06-20

### 新增：血条材质选框 + 颜色取色器（外观页）

常规 → 外观 下新增两个选项：

- **血条材质**：带预览的材质下拉选框（基于 LibSharedMedia）。
- **血条颜色**：取色器，自定义 boss 血条填充色。

默认材质与颜色等同旧实现，**升级后零视觉变化**。配置随 `MBT1!` 字符串导入导出，格式向后兼容。

### 新增：术士专属光圈设置（常规页）

常规 → 术士专属设置 下，给余烬 / 暗影印记双 6 层的整框光圈加了两个选项：

- **光圈样式**：像素描边 / 自动施法流光 / 按钮高光 / 脉冲流光。
- **光圈颜色**：取色器。
- **光圈转速**：调动画快慢（三种样式通用）。
- **线条粗细 / 流动点数**：像素描边的变体（仅选像素样式时显示）。
- **光点大小 / 光点数量**：自动施法流光的变体（仅选自动施法样式时显示）。光点默认放大，解决原先光点太小几乎看不见的问题。

默认全部等同旧实现，升级后零视觉变化。

## 1.13.0 — 2026-06-16

### 改进：术士 Haunt 续断标记改用实时测距

引导条上的 Haunt（续断）续命标记，原先按固定 30 码估算飞行时间，现在改成**实时读取与目标的距离**：

- 优先用 **WeakAuras.GetRange**（和 Fojji 那条 WA 用的是同一个函数，读数完全一致）；
- 没有就退回任意插件注册的 **LibRangeCheck-2.0** 共享实例；
- 环境里完全没有测距来源时，才回退到原来的固定估算值。

距离越准，飞行时间越准，续断标记的落点也越贴合实战。

## 1.12.0 — 2026-06-13

### 新增：暗牧「暗言术：渊」命中刷新 DoT

点了天赋「坍缩深渊」后，暗言术：渊造成伤害会刷新目标身上的**暗言术：痛**和**吸血鬼之触**持续时间——现在框体里这两条 DoT 会跟着一起刷新，机制同痛苦术暗影箭刷新腐蚀术。

### 修复：雷霆崖训练假人不显示

雷霆崖的训练假人在**猎人高地**，原数据误写成「艾森娜高地」，导致在正确位置反而不出框。已修正。

## 1.11.0 — 2026-06-10

### 改进：DoT 图标调大不再撑大整框

调大 DoT 图标时整框越变越宽的问题修掉了，两种样式各按自己的逻辑：

- **标准样式**：血条宽度固定（= 默认尺寸下的原宽，默认观感不变），DoT 图标变大只会向下挤压血条高度、不再撑宽整框。图标越大、固定宽度里能并排的 DoT 越少，选项里实时提示「当前大小最多能放下几个 DoT」。
- **极简样式**：DoT 区固定统一长度（与旧版一致）。此样式下「DoT 大小」就等于整条尺寸、与「整体缩放」完全等效，已隐藏多余的 DoT 大小滑条，只保留「整体缩放」。

> 默认设置下，框体宽高与可显示的 DoT 个数都和上一版一致；只有手动把 DoT 调大到默认值以上的玩家会看到上述新行为。

## 1.10.1 — 2026-06-08

### 修复：多座主城训练假人不显示

子区域名跟客户端实际返回值对不上，导致进城找不到假人：

- **达纳苏斯**：战士区（原数据写的是「战士露台」，客户端实为「战士区」）。
- **暴风城**：假人在「军情七处」（SI:7），原来只注册了「旧城区」——而旧城区本身没有假人。

### 修复：铁炉堡宗师假人开关绑错

大锻炉的**宗师假人**显隐开关误绑到了「专家假人」开关上，现已绑回自己的宗师开关。

### 修复：冰霜疫病 DoT 颜色笔误

蓝色通道 `9.247`（非法值）→ `0.947`，死亡骑士冰霜疫病 DoT 颜色恢复正常。

### 清理

移除 BossFrame 里被后续定义遮蔽的一组重复血条颜色常量（无行为变化）。

## 1.10.0 — 2026-06

### 新增：选中高亮可选「蓝色光晕」

设置面板「外观」里，紧跟「样式」右边新增**选中高亮**下拉：

- **黄竖线**（默认）：当前目标的 boss 框左侧一道黄色竖线，和以前一样。
- **蓝色光晕**：沿整个框体（头像 + 血条 + DoT）外缘一圈蓝色光晕，比黄竖线更醒目。

### 调整：阵营冠军赛的配色

十字军试炼 PvP 阶段（阵营冠军赛）专属：

- 名字前的「天赋 + 职业」前缀不再单独用职业色，**跟随 boss 名字字体颜色**。
- **血条改用职业颜色**（法师浅蓝、萨满蓝、术士紫等）替代默认红色，一眼区分职业。

### 修复：Boss / 小怪死亡后状态没清理

- 单位死亡后**团队标记残影**会一直留着，团长再标记别的目标就会出现两个标记 —— 现在死亡即清除。
- 单位死亡后**术士印记层数**会一直保留，甚至串到下一场战斗显示错误层数 —— 现在死亡即归零，换区域 / 换 boss 也会彻底清空。

## 1.9.0 — 2026-06

### 新增：整体缩放

设置面板「外观」里新增**整体缩放**滑块（50%–200%），把整个面板一起放大或缩小 —— 血条、头像、DoT、文字全部等比缩放，面板位置保持不变。想让画面整体变大变小、又不想一项项调的，用这个。

### 调整：DoT 图标大小不再撑大整个框体

以前把 DoT 图标调大，整个 boss 框（血条高度、头像、总宽）会跟着变大。现在**框体大小固定不变**：DoT 图标越大，上方血条就越窄（向下挤压血条），直到血条压到刚好能显示文字为止（即图标的最大上限）。想等比放大整个面板请改用上面的「整体缩放」。

### 调整：设置项顺序

把「整体缩放」和「DoT 图标大小」挪到「外观」一栏最前面，紧跟在「样式」后面，常用的尺寸调节一眼就能找到。

## 1.8.1 — 2026-06

### 调整：新版本提示的更新渠道

「有新版本可用」的聊天提示改为指引玩家从**新手盒子**或**网易DD**更新（v1.8.0 里指向的是 GitHub 下载页，国内玩家不方便访问）。

## 1.8.0 — 2026-06

### 新增：新版本提醒

当队伍 / 团队 / 公会里有人在用更新版本的本插件时，聊天框会提示一次新版本号和更新渠道，20 小时内最多提示一次。原理是通过游戏内的插件通信频道交换版本号 —— 插件本身没有任何联网行为，消息只有十几个字节，登录和进组时各发一次，不影响性能。

### 调整：拖动条跟随区域显隐

解锁状态下的拖动条（标题栏）现在只在当前区域有 boss 数据时显示（假人区、副本等），离开这些区域后跟框体一起隐藏，不再孤零零悬着一个条。注意：在没有 boss 数据的区域解锁时拖动条不会出现，需要走到假人区/副本里才能拖动。

## 1.7.5 — 2026-06

### 新增：透明模式

常规 → 外观新增「透明模式」开关。勾选后按区域生效：DoT 显示区域和框体缝隙的黑底完全透明（不再挡住后面的场景/单位）；血条变为半透明（掉血部分能清楚透出场景、红色填充稍微透一点）；头像、术士印记行、吸取灵魂引导条保留实心黑底，保证读数清晰。标准和极简样式都生效，战斗中也可切换。

### 新增：术士「暗影掌握」DoT 追踪 (17800)

暗影箭命中后给目标挂的暗影掌握 debuff 现在会显示为 DoT 图标（暗影箭图标、紫色边框）。可在「DoT 排序」里指定槽位，也可在 DoT 黑名单里关闭。持续时间优先读取目标身上的真实数据。

### 调整：Boss 框体间距默认值 40 → 20

仅影响没手动调过该滑动条的配置；调过的保持原值。

## 1.7.4 — 2026-05

### 新增：DoT 倒计时字号可设为 0（隐藏数字）

「DoT 倒计时字号」滑动条最小值从 8 改为 0。设为 0 时完全隐藏插件自绘的倒计时数字，适合使用 OmniCC 等冷却数字插件的玩家 —— 此前 OmniCC 的数字会和插件自绘的数字叠在一起，现在设 0 后只剩 OmniCC 的倒计时。

## 1.7.3 — 2026-05

### 修复：奥根（曼多基尔坐骑）无法点击选中

祖尔格拉布血领主曼多基尔的坐骑「奥根」(Ohgan) 框体能显示血量和 DoT，但点击框体切不了目标。原因是 zhCN 译名写成了「奥甘」，跟客户端实际单位名「奥根」差一个字 —— 点击宏 `/targetexact 奥甘` 精确匹配不到单位。DoT 靠 NPCID 匹配所以不受影响。改正译名即可。

## 1.7.2 — 2026-05

### 修复：boss 框团队标记从来不显示

团长打在 boss 上的团队标记（骷髅 / 月亮 / 菱形…）一直没出现过。根因是本客户端（5.4.3）的 `SetRaidTargetIconTexture` **只设裁剪坐标、不设贴图文件** —— 而自建的标记贴图从没挂过团标图集，等于在一张空贴图上设坐标，框在显示、尺寸和透明度都正常，但内容为空，什么都画不出来。创建时补上图集文件即可。

### 调整：团标位置移到框体左外侧

标准模式下团标原本贴在头像左上角，容易被 3D 头像挡住。现在两种模式统一显示在**框体左外侧、当前目标黄色竖线的左边**，更清晰不遮挡。

## 1.7.1 — 2026-05

### 修复：zhCN 客户端怪物译名与点击无反应

之前两处译名与你客户端实际单位名差一截，点击宏 `/targetexact <名字>` 找不到单位，框体点了没切目标：

- **狗头人奴隶 → 雪地狗头人奴隶**（Snobold Vassal，十字军的试炼 / 戈莫克身上跳下来的）
- **狂热者罗尔卡恩 → 狂热者洛卡恩**（Zealot Lor'Khan，祖尔格拉布 / 塞卡尔小弟）

### 调整：高阶祭司耶克里克的两只蝙蝠小怪移除

战斗中召唤的嗜血蝙蝠 / 狂乱的嗜血蝙蝠数量太多，没追踪价值，去掉。

## 1.7.0 — 2026-05

### 新增：阵营冠军赛 — 动态阵容显示

十字军的试炼「阵营冠军」每周随机刷一部分职业代表，以前框体写死、刷出来的反而看不到。现在改成动态识别：

- **只显示实际刷出来的冠军**，靠敌方名牌检测，进房间看一眼（让冠军都出名牌）就自动占框
- boss 名字前加 **[天赋+职业]** 前缀并按职业颜色上色，例如紫色 `[术士]`塞瑞莎·术轮、`[增强萨满]`/`[恢复萨满]` 一眼区分同职业不同天赋
- boss 框上限从 5 个提到 10 个，25 人本最多 10 个冠军也放得下；常规 boss 不受影响（用不到的框保持隐藏）

注意：受暴雪安全框限制，名单在**开打瞬间冻结** —— 进房间先看一眼让冠军都出名牌、再开最稳。需要开启「显示敌方名牌」。

### 新增：祖尔格拉布 — 全副本 boss 框支持

ZG 现在按**子区域**自动出框，走进哪个 boss 房间就显示哪只，不用手动悬浮：

- 耶克里克 / 温诺希斯 / 玛尔里 / 曼多基尔 / 塞卡尔 / 娅尔罗 / 金度 / 哈卡 各自房间自动出框
- **疯狂之缘 4 选 1**：每周只刷一个，动态识别只显示实际刷出来的那只巨魔
- 加兹兰卡（所在区域无独立子区域名）靠名牌/悬浮自动识别

### 新增 / 调整：加拉克苏斯大王房间

- 补回 **魔焰地狱火**（地狱火山召出来的小地狱火，真正能打的小怪）
- 移除「虚空传送门」「地狱火山」两个召唤源（本身不是攻击目标）
- 显示顺序：加拉克苏斯 → 痛苦女王 → 魔焰地狱火

### 修复：部分副本区域不显示框体

子区域名在数据集里没命中时，现在会退回主区域名匹配（修复祖尔格拉布等「子区域有名字、但 boss 数据按主区域建」的副本不出框）。

### 调整：北裂境野兽群

狗头人奴隶（戈莫克身上跳下来的小怪）移到穿刺者戈莫克正后面显示。

## 1.6.1 — 2026-05

### 撤回：v1.6.0 的 DoT 点击模式

经过实际使用，DoT 点击模式带来的复杂度超出收益，回滚整套功能。点击施法回到 v1.5.x 的纯修饰键模式。

如果你之前更新到了 v1.6.0 并启用了 DoT 点击模式，更新到 v1.6.1 后行为会自动回到经典模式，不需要任何手动操作。

其它 v1.5.x 的功能（印记层数指示条 + 吸取灵魂引导条）保持不变。

## 1.5.1 — 2026-05

### 微调：吸取灵魂引导条 delta 数字加 DPS 单位
- 触发 proc / buff / 急速变化时显示从 `▲ 500` → `▲ 500 DPS`、`▼ 250` → `▼ 250 DPS`
- 加单位更直观，看一眼就知道是 DPS 增减

## 1.5.0 — 2026-05

### 新增：毁灭术士 — 余烬/暗影印记层数指示条

boss 框底部新增一行 6 格指示条，跟盗贼连击点一个风格，扫一眼就能看出两个印记当前层数：

- 每格上半段是**余烬印记橙色**、下半段是**暗影印记紫色**，独立点亮代表各自层数
- 左侧两个数字 `余烬/6` `暗影/6` 同步显示精确层数
- 双 6 时整个 boss 框出现橙色流光，提示进入终结技窗口

### 新增：痛苦术士 — 吸取灵魂引导条

boss 框底部新增一条引导进度条 + 决策辅助，帮你判断该断该续：

- **倒计时进度条** 从右往左缩，对应当前引导剩余时间
- **4 条竖线** 把条切成 5 段，对应吸取灵魂 5 跳伤害的时机点
- **右端实时闪伤害数字**，每跳出多少一目了然
- **红色竖线** 显示"该断了去续 Haunt"的最佳位置（综合 Haunt 剩余时间 + 你到 boss 的距离 + 网络延迟自动算出）
- **左侧文字智能切换**：
  - 平时显示「吸取灵魂」
  - 触发饰品 / 吃 Buff / 急速 proc 等让 DPS 提升时 → 绿色 **▲ +X DPS**，提示该断了重新引导抢快照
  - 引导期间 cast 时的 Buff 过期了，DPS 下降 → 红色 **▼ -X DPS**，提示别断继续吸

### 智能联动

- **自动跟随天赋**：切到毁灭看印记条；切到痛苦看引导条；其它天赋什么都不显示，boss 框恢复原始高度
- **切天赋自动清场**：换天赋瞬间，旧天赋打过的 DoT 图标立刻清掉，不留残影

### 可关

DoT 黑名单 → 术士 组新增两个开关：
- 「隐藏印记层数指示条」—— 不喜欢印记 UI 的勾上
- 「隐藏吸取灵魂引导条」—— 不喜欢引导条的勾上

### 视觉优化

- 两个 UI 都做了像素级对齐：标准模式跟头像 + HP 条对齐，紧凑模式跟血条 + DoT 区对齐
- 引导条紫色调亮偏蓝，暗副本里更清晰
- 层数指示条 tick 分隔线粗细统一、行高微调

## 1.4.1 — 2026-05

### 修正：暗牧 Devouring Plague 标签
- "吞噬瘟疫" → "噬灵疫病"（对齐国服怀旧服译名）

### 发布流程改进
- Release 现在附带打好的 zip（根目录直接叫 `MultiBossTracker/`），解压进 AddOns 即用
- 之前下载 GitHub Source code zip 的用户：解压后文件夹名带版本号（如 `MultiBossTracker-1.4.0`），WoW 不认，需要手动改名为 `MultiBossTracker`；v1.4.1 起改用 Actions 自动打包的 release asset，不再有此问题

## 1.4.0 — 2026-05

### 新增：P4 阶段副本数据（十字军的试炼 + 祖尔格拉布）
- 服务器开放 P4，原 `T9.lua` 重命名为 `T4.lua`（文件命名跟随服务器 phase 而非客户端 tier）
- 新增祖尔格拉布（Vanilla 20 人团本）9 个 boss + 疯狂之缘 4 选 1：
  - 高阶祭司耶克里克 / 温诺希斯 / 玛尔里 / 塞卡尔 / 娅尔罗
  - 血领主曼多基尔（含坐骑奥甘）
  - 疯狂之缘四选一：格里雷克 / 哈扎拉尔 / 雷纳塔基 / 乌苏雷
  - 加兹兰卡 / 妖术师金度（含金度的影像）/ 哈卡（含哈卡之子）
  - 嗜血蝙蝠、狂乱的嗜血蝙蝠、狂热者罗尔卡恩 / 扎斯 等小怪也一并追踪
- Vanilla 副本无官方 EJ 编号，ENCOUNTER_END 大概率不触发；走 Naxx 那套 mouseover 驱动模式
- 奥妮克希亚的巢穴暂时注释掉（P4 不开放，等服务器后续公告）

### 新增：「隐藏单位」标签补 P4 项
- 设置 → 隐藏单位 加 P4 — 十字军的试炼 / 祖尔格拉布 分组
- 阵营冠军赛 14 个职业用「一勾控同职业部落+联盟双方」写法
- 疯狂之缘 4 个备选巨魔合并一勾

### 修正：祖尔格拉布译名对齐国服怀旧服官方
- 译名按国服怀旧服 / 17173 / dailianqun 攻略统一：
  - 玛尔莉 → 玛尔里、赛卡尔 → 塞卡尔、阿洛克 → 娅尔罗、洁克丽克 → 耶克里克
  - 邪术师金多 → 妖术师金度、加兹拉卡 → 加兹兰卡
  - 疯狂之痕 → 疯狂之缘、狂怒的嗜血蝙蝠 → 狂乱的嗜血蝙蝠

## 1.3.2 — 2026-05

### 改进：Naxx 各翼 boss 不再一起显示
- 蜘蛛 / 瘟疫 / 军事 / 构造 四个翼改用 Encounters 链路（同毒蛇神殿那套）
- 进翼时不显示框，鼠标悬浮哪只 boss 才出哪只
- 杀掉 boss → ENCOUNTER_END 自动推进到下一只（按默认击杀顺序）
- 天启议事厅 / 萨菲隆之巢 / 克尔苏加德的大厅 维持独立 zone（这些有专属子区域）

## 1.3.1 — 2026-05

### 新增：团标显示
- boss 被团长打了星星 / 圈圈 / 菱形等标记时显示
- 标准模式：贴在头像左上角，1/4 头像大小
- 紧凑模式：贴在 boss 框左外侧、当前 target 黄竖线的左边
- 设置 → 显示内容 → 「显示团标」开关，默认开启
- 监听 `RAID_TARGET_UPDATE`，团长改标记时立刻刷新（不用等血量 tick）

### 修复：Naxxramas 子区域匹配
- 「构造区」之前误写成「建筑区」，导致进 Patchwerk / Grobbulus / Gluth / Thaddius 房间时不显示框体
- 冰霜巨龙翼实际有独立的 boss 房子区域，拆分成「萨菲隆之巢」和「克尔苏加德的大厅」两个独立 zone 条目，各自只显示对应 boss

## 1.3.0 — 2026-05

### 重大改动：UI 全面像素化重构
- 整个 boss 框体重新设计成"像素硬核"风格：1px 黑硬边框 + 满色无渐变 + 高对比饱和色
- HP 条颜色改成饱和深红 #C72E33（之前的暗红 #9F0000 在战斗背景下偏闷）
- Boss 施法条改成暖琥珀金 #F5C645（之前的金色偏暗）
- 玩家施法条 1px 青色细线贴 HP 底
- 所有元素都内缩 1px，靠纯黑底从边缘漏出形成"硬边框"质感

### 重大改动：DoT 大小驱动整框尺寸
- 极简模式：行高 = DoT + 2px 边，HP 高度跟 DoT 同步
- 标准模式：HP 高度 = max(30, DoT)，HP 宽度 = max(200, DoT 行宽度)，头像方块 = 行高 - 2
- DoT 调大 → 整个 boss 框按比例放大（行高 / HP 大小 / 头像 / 总宽都自动跟着涨）
- DoT 范围扩展为 16-48px（之前是 20-48）

### 移除：「主题」概念
- 之前短暂引入过的「主题」(经典 / 像素) 概念取消，像素成为唯一样式
- 设置面板里的「主题」下拉框删除
- `sTheme` 字段从默认值移除；老 profile 里残留此字段不影响（被忽略）
- 老的 `MBT1!` 导出字符串仍可导入（多余字段被忽略）

### 数据：Naxxramas 重归 T3 + Phase 3 阶段调整
- `T7.lua` → `T3.lua`（按 Vanilla 时序，纳克萨玛斯本就是 T3；服务器把 Naxx + 黑曜石圣殿 + 永恒之眼打包到 P3 阶段）
- 数据集英文 key 改用各 Naxx 翼的子区域名（"The Arachnid Quarter" 等），匹配实际 `GetSubZoneText()` 返回值
- Locale 补全 Naxx 各区域和 boss 的中文翻译
- Testing zone：新增"远行者广场"训练假人位置（银月城另一个常去的地方）
- 移除"克拉苏斯平台"训练假人（达拉然在当前阶段不可达）
- toc 注释掉 T8 / T9 / T10（副本未开放，开放后取消注释即可）

### 修复：HP target 高亮回退到旧色
- 选中 boss 框体时血条会变亮，离开后回到旧的暗红 #9F0000，跟新像素色 #C72E33 对不上
- 现在 normal / highlight 颜色都用新的像素红，过渡一致

### 选项联动
- 「DoT 图标大小」始终可调（之前在某些组合下被隐藏）
- 「行间距」在标准模式下始终可调（极简模式仍固定 6px 紧凑）

## 1.2.0 — 2026-05

### 新增：配置档导入 / 导出
- 把当前配置档序列化成 base64 字符串发给朋友，对方粘贴即可导入
- 设置 → 配置档 → 末尾「打开导出窗口 / 打开导入窗口」两个按钮
- 同名配置档导入时直接覆盖；不同名的会新建一档并自动切换过去
- 字符串格式带 `MBT1!` 前缀，未来真有不兼容改动时会 bump 版本

### 修复：玩家施法条 spam 误关
- 读条过程中狂按其它施法技能，被拒的 cast 抛出的 `UNIT_SPELLCAST_FAILED` 会把当前正在跑的施法条视觉收掉
- 现在用 castGUID 精确匹配，只对当前显示的那次 cast 的事件做反应，其它 cast 的事件忽略

### 修复 + 改进：极简模式 UI 一致性
- **修复**：极简模式下 boss 读条时，施法条会以"标准模式尺寸"挤进 DoT 行（设计本意是极简模式无 boss 施法条）。现在按设计契约 no-op
- **改进**：选了极简样式后，与之无关的选项（DoT 图标大小 / 行间距 / 3D 头像 / Boss 施法条）会自动从设置面板隐藏；切回标准重新出现。设置值本身保留，不会丢

### 改进：样式名改成更易理解的中文
- 「紧凑 / 完整」 → 「极简 / 标准」（少点机翻味）
- `iSkin` 内部值不变，老配置档和老的 `MBT1!` 导出字符串完全兼容

### 文档
- README 加入界面预览（极简 / 标准对比图 + 动图链接）
- 加入设置面板六张功能截图：总设置、DoT 排序、DoT 黑名单、点击施法、隐藏单位、配置保存与分享

## 1.1.0 — 2026-05

### 新增：玩家自己的施法条
- 在每个 boss 框体血条最底叠加一条 4px 高的青色窄条，表示"我当前的 cast 正对着这只 boss"
- 多目标 dot 切换时一眼定位 cast 目标，不再手忙脚乱漏 cast
- 有读条法术（暗影箭、鬼影缠身）→ 进度填充；瞬发（腐蚀、痛苦诅咒）→ 闪 0.25s；引导（汲取灵魂）→ 反向递减
- Boss 施法条同时显示时上下 stack 不冲突（玩家青条覆盖 boss 黄条底 4px，boss 条上方 6px 仍可见，施法名自动上移避免被压）
- 设置 → 常规 → 显示内容 → 显示自己的施法条 关闭

### 新增：距离 / 朝向提示
- HP 条内嵌右上角显示鲜黄色状态文字
- **超出范围** —— boss 不在你最远技能射程内（持续显示）
  - 基于职业最远射程代表技能：暗影箭 / 寒冰箭 / 心灵震爆 / 闪电箭 / 自动射击 / 愤怒 / 死亡缠绕
  - 自动算上天赋 / 雕文 / buff 加成（IsSpellInRange API 是 live 的）
- **面对目标** —— 你点击施法但游戏返回 facing 错误（闪 1.5s）
  - 持续狂点会持续续期（绕过 WoW 引擎对重复错误信息的节流抑制）
  - 成功施法后立即清除（避免事后假阳性）
- 设置 → 常规 → 显示内容 → 两个独立开关，默认开

### 新增：术士印记追踪
- 追踪两个印记 debuff：余烬印记（spellID 1295144）/ 暗影印记（spellID 1295140）
- 默认显示在 DoT 行最前面（位置 1、2）
- **单印记到 6 层** → DoT 图标周围金色旋转火花特效（信息性，"这边准备好了"）
- **双印记同时到 6 层** → 整个 boss 框体周长 8 颗暖橙色像素点流动（行动性，"终结技窗口开了"，2 秒一圈）
- 任何一边掉到 5 层 → combo 流光立即消失
- 黑名单 tab 有这俩条目可独立屏蔽

### 新增：DoT 图标大小可调
- 设置 → 常规 → 外观 → "DoT 图标大小（像素）" 滑动条
- 20-48px，步长 2，默认 32，实时生效
- 紧凑模式受行高 26px 物理约束，不受此设置影响

### 新增：第三方库 LibCustomGlow-1.0
- 完整引入 WeakAuras / ElvUI 通用 glow 库（`Libs/LibCustomGlow-1.0/`），未做修改
- 提供 4 种 glow 风格：PixelGlow / AutoCastGlow / ButtonGlow / ProcGlow
- 配套 Compat.lua polyfill 顶住 MoP 客户端缺失的 `CreateTexturePool` / `CreateFramePool`（Legion+ API）
- 未来其它信号（pandemic / 低血 / cast target / 处决线等）可零成本扩展

### 改进：DoT 元数据 runtime 化
- DoT 图标 / 持续时间 / 当前层数现在从 `UnitDebuff` 实时读取，静态配置仅作 fallback
- 新增 DoT spell 时不再需要手动查 icon FileDataID
- 修复：初始 SPELL_AURA_APPLIED 时如 boss 身上已多层（部分技能首次 apply 自带层数），层数显示能立即正确

### 改进：Combo Glow 数据驱动
- 各职业 `tAuraData.tComboGlows` 配置项支持"多个 DoT 同时满足 → 整框 glow"规则
- 加新 combo 零代码改动，配置即生效
- 每条 combo 自定义 glow 风格 / 颜色 / 频率 / 阈值

### 新增：术士其它 DoT
- 暗影灼烧（Shadowburn, spellID 29341）
- 燃烧（Conflagrate, spellID 17962）
- 都加到 DoT 排序下拉 + 黑名单选项中，可独立屏蔽 / 自定义槽位

### 改进：DoT 黑名单按职业分组
- 黑名单 tab 不再是单一长平铺列表，按术士 / 法师 / 牧师 / 德鲁伊 / 萨满 用 header 分组
- 同职业的 DoT 集中在一段，找自己用的更快
- 数据结构从两份并行表（SPELL_BLACKLIST_KEYS + SPELL_BLACKLIST_LABELS）整合成 SPELL_BLACKLIST_BY_CLASS 单表，加新 DoT 一行搞定

### 修复
- Hunter.lua "爆炸陷阱" 内部 key 统一为英文 "Explosive Trap"（玩家可见显示无变化）
- DoT 黑名单中文翻译修正：
  - 末日诅咒 → **厄运诅咒**
  - 不稳定的痛苦 → **痛苦无常**
  - 暗影缠绕 → **暗影之拥**
  - 吸血鬼之触/吞噬瘟疫 拆分为两个独立条目（牧师两个不同 DoT 不应合并）

## 1.0.0 — 2026-05

### 核心功能
- 多目标 boss 框体（最多 5 个：A/B/C/D/E），自动识别区域 + 遭遇战
- 点击施法：左键 / 右键 / 中键 / 侧键 4 / 侧键 5 + Shift / Ctrl / Alt 修饰，**不切换当前目标**
- DoT / 施法 / HP 监控独立分发
- 7 个职业的 DoT 数据（术士、法师、死亡骑士、牧师、德鲁伊、萨满、猎人）
- 7 个副本数据（T1 MC / T2 TK+SSC / T7 Naxx+OS / T8 Ulduar / T9 ToC / T10 ICC / Testing）
- 训练假人覆盖 6 大主城（奥格 / 铁炉堡 / 银月城 / 暴风城 / 雷霆崖 / 达纳苏斯 / 达拉然）

### UI
- 紧凑 / 完整 两种外观可切换
- 3D 实时 boss 模型（可切换为 2D 静态头像以省 GPU）
- DoT 图标自渲染居中倒计时数字（不被 swipe 遮罩压暗），字号可调
- HP 条满高填充、施法条叠在底部 OVERLAY 层，无空白
- 当前 target 框体高亮：HP 变亮 + 左侧金色竖条
- 拖动条改用 WoW 标题栏样式，鼠标悬停高亮

### 设置 / 配置
- AceConfig 设置面板：常规 / DoT 黑名单 / 点击施法 / 调试 / 配置档
- 多角色 / 多 profile 切换时，所有运行时状态（安全宏、UI、锚点）自动同步刷新
- 拖动位置以 UIParent 中心偏移保存（跨分辨率稳定）

### 命令
- `/mbt` — 打开设置
- `/mbt where` — 打印当前区域和目标 NPCID
- `/mbt showmacro [A-E]` — 打印某个框体的点击施法宏
- `/mbt lock` / `/mbt unlock` / `/mbt reset` — 控制位置

### 小地图按钮
- 默认在小地图边上显示一个图标（猎人标记图标）
- 左键 → 打开设置面板
- 右键 → 切换框体锁定 / 解锁
- 可以在 设置 → 常规 → 显示小地图按钮 关闭

### Bug fixes
- 战斗中切换紧凑/完整外观、改行间距、改反向排列等会导致 SecureActionButton 无法被 SetSize/SetPoint，框体出现"半新半旧"的畸形状态。现在这类操作会自动排队，等战斗结束的瞬间统一应用，无感等待

### 隐藏单位
- 新增"隐藏单位"标签：按服务器版本阶段 P1 / P2 / P3 组织
  - P1 — 熔火之心
  - P2 — 风暴要塞 / 毒蛇神殿
  - P3 — 纳克萨玛斯 / 永恒之眼 / 黑曜石圣殿
- 每个阶段下所有 boss 平铺成一个个独立勾选框，按 NPCID 去重，不暴露后台的 zone/encounter/slot 概念
- 适用场景：boss 战里的同名小怪占框体但点击施法无效（受 WoW 重名 NPCID 限制），勾掉这些只保留 boss 本体
- 修改实时生效，框体立即重排
- T3+ 之后的副本暂未列入 UI（数据保留，等服务器开版本时再启用）

### DoT 排序自定义
- 设置面板新增"DoT 排序"标签，每职业独立配置
- 各位置（槽位 1 = 最左）下拉选择放哪个 DoT
- 改完立刻生效，已经在 boss 身上的 DoT 也会一起重新排序
- 防呆：同一 DoT 不允许出现在多个槽位 —— 选到新槽位时旧槽位自动清空
- 修复：原算法用 `pairs()` 顺序写入 iOrder，重复设置时实际生效的优先级由哈希顺序决定，违反直觉。现改成"最低槽位号优先"，符合"槽位 1 是最高优先级"的一致认知

### 默认值调整
- 血量刷新间隔默认值从 1.0 秒 → 0.1 秒。DPS 输出时血量是关键判断依据（处决线、阶段切换、目标转火时机），低延迟体验更顺手。需要省性能可在 设置 → 常规 → 显示内容 调高。
  - 注：仅对**新建 profile** 生效。已有 profile 保留之前的值，需手动改

## 已知限制

- **重名目标**：两个 NPCID 相同的单位无法分别点击施法（受 WoW 安全宏 `/targetexact <name>` 限制，无法按 GUID 定位单位）。HP / DoT 跟踪也会"互相覆盖"。这是 WoW 引擎的硬约束，所有第三方插件都无法绕开。
- **天赋 / 雕文加成**：MoP API 与 WotLK 不同，目前 stub 为 `false`，DoT 时长用基础值（不影响 DoT 监控本身，仅时长可能略偏长）。
