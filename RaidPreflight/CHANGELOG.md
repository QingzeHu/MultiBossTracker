# 更新日志

## 0.4.0 — 2026-06-25
- 暴雪原生配置面板（无 Ace）：总开关 / 三触发器 / 进本冷却滑条 / 重置记录
- 检查清单编辑器：按作用域（通用 / 副本 / 副本+boss）增删检查项；类型支持物品、光环关键字、团队职业（可带专精）
- 导入 / 导出分享：用户配置序列化成 `RPF1!` 分享串，原生多行框复制粘贴
- 修正 base64 解码越界截断（载荷长度非 3 倍数时丢尾字节）

## 0.3.0 — 2026-06-25
- Boss 顺序复用 MultiBossTracker：运行时读其已本地化的 `localizedZones`（装了即零拷贝复用全部副本/顺序），未装则回退自带种子
- 采用 MBT 数据形状（Encounters + iStartingFight + iNextEncounter），推断逻辑照搬（子区域 > 主区域 → 当前 encounter → 鼠标/名牌 NPCID 覆盖）

## 0.2.0 — 2026-06-25
- 开战即自动消失（PLAYER_REGEN_DISABLED），绝不干扰开战
- 团队专精检测（inspect 管理器）：comp 检查支持 `aSpecIDs`，要求职业在场 + 专精匹配；读不到标「专精待确认」
- 天赋点不做：MoP 5.5 已无被检视单位的可靠天赋读取，只做到专精粒度

## 0.1.0 — 2026-06-25
- 首个骨架：三触发器（召唤拦截 / 进本一次 / 开战前 ready check）
- 检查引擎：自身物资（GetItemCount）、自身增益（UnitAura）、团队职业构成
- 可复用清单弹窗（绿勾 / 黄警告 / 红叉）
- 模板三级覆盖（通用 → 副本 → boss）
